# ── MIMIC-IV PhysioNet data (credentialed access required — do not redistribute) ──
data/mimic/

# MIMIC-derived assets in task container (built from PhysioNet data)
task/assets/mimic_*.pkl
task/assets/mimic_train.csv
task/assets/mimic_test.csv

# ── METR task-standard (cloned locally for development — users install separately) ──
task-standard/

# ── API keys and secrets ──
.env
.env.*
*.key
secrets/

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
dist/
build/

# ── Evaluation results (may contain model outputs derived from restricted data) ──
evaluation/results/

# ── Manuscript / paper (local working copy — not distributed with the task code) ──
manuscript/

# ── OS / editor noise ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
